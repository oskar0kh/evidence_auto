# Gradle 8.11.1은 JDK 24 이상에서 자체 실행이 불가능합니다(지원 범위: JDK 8~23).
# 이 컴퓨터의 기본 java 명령이 JDK 25를 가리키고 있어, Gradle 실행에 사용할 JDK를
# 이미 설치되어 있는 JDK 21로 고정합니다.
org.gradle.java.home=/usr/lib/jvm/java-21-openjdk-amd64
